import React, {useEffect, useRef, useState} from 'react';

const BlockBody = ({ children, title }) => {

    return (
        <div style={{ flex: 1, display: "flex", backgroundColor: "black"}}>

        </div>
    );
};


export default BlockBody;
